(()=>{function setStatus(card,text){if(!card)return;const spans=[...card.querySelectorAll('.meta span')];const status=spans.find(x=>/^(準備中|待機中|実行中|完了|失敗)$/.test((x.textContent||'').trim()));if(status){status.textContent=text;status.className=`job-status ${typeof h3StatusClass==='function'?h3StatusClass(text):''}`.trim();}}function terminal(item){const msgs=item?.status?.messages;if(Array.isArray(msgs)){for(const m of msgs){if(Array.isArray(m)&&['execution_success','execution_error','execution_interrupted'].includes(m[0]))return m[0];}}if(item?.status?.completed===true)return 'execution_success';const outputs=item?.outputs||{};const hasOutput=Object.values(outputs).some(node=>['videos','images','gifs','files','audio'].some(k=>Array.isArray(node?.[k])&&node[k].length));return hasOutput?'execution_success':null;}function secondsFrom(item,fallback){if(typeof historyExecutionSeconds==='function'){const s=historyExecutionSeconds(item);if(s!=null)return s;}return fallback;}function au(path){return typeof apiUrl==='function'?apiUrl(path):path;}
async function tick(){if(typeof state==='undefined'||!state.jobs)return;const active=[...state.jobs.entries()].filter(([id,job])=>job&&!job.done);if(!active.length)return;let running=new Set(),pending=new Set();try{const qr=await fetch(au('/queue'));if(qr.ok){const q=await qr.json();(q.queue_running||[]).forEach(it=>{if(Array.isArray(it)&&it[1])running.add(it[1]);});(q.queue_pending||[]).forEach(it=>{if(Array.isArray(it)&&it[1])pending.add(it[1]);});}}catch{}for(const [id,job] of active){if(running.has(id)){if(!job.executionStartedAt)job.executionStartedAt=Date.now();job.phase='running';setStatus(job.card,'実行中');continue;}if(pending.has(id)){job.phase='queued';setStatus(job.card,'待機中');continue;}try{const r=await fetch(au('/history/'+encodeURIComponent(id)));if(!r.ok)continue;const data=await r.json();const item=data?.[id]||Object.values(data||{})[0];if(!item)continue;const t=terminal(item);if(!t){if(!job.executionStartedAt)job.executionStartedAt=Date.now();job.phase='running';setStatus(job.card,'実行中');continue;}job.done=true;job.result=t;const fallback=Math.max(0,(Date.now()-(job.executionStartedAt||job.startedAt))/1000);const sec=secondsFrom(item,fallback);const el=job.card?.querySelector('.elapsed');if(t==='execution_success'){setStatus(job.card,'完了');if(el)el.textContent='生成 '+formatDuration(sec);}else{setStatus(job.card,'失敗');if(el)el.textContent='停止 '+formatDuration(sec);}}catch{}}}setInterval(tick,2000);})();
